# CLAUDE.md — 펫포커스

이 파일은 이 저장소에서 작업하는 AI 어시스턴트(및 사람)를 위한 단일 기준 문서다.
`[HARD RULE]` 표시된 항목은 사용자 승인 없이 절대 어기지 않는다.

---

## 0. 이 저장소의 구성 (펫포커스 도구 모음)

2026-07-19 정리로, 흩어져 있던 펫포커스 도구를 이 저장소 하나로 모았다.
**도구 1개 = 폴더(또는 루트) 1개 = HTML 파일 1개** 원칙이며, 각 도구는 서로 독립적이다.

| 경로 | 도구 | 문서 |
|---|---|---|
| `index.html` | **현장재고** — Firestore 기반, 재고/입출고/이력/원가 + 원재료 재고·생산배치 원가·수율 (2인 현장용) | 아래 §1~§8 |
| `detail/index.html` | **상세페이지 프롬프트 생성기** — 상세페이지용 AI 프롬프트 6단계 생성 | `detail/GUIDELINES.md` |
| `docs/` | 펫포커스 마케팅 카피·기획 문서, 작업 계획서(`plans/`) | — |
| `.claude/skills/marketing` | 마케팅 톤앤매너 규칙 | — |
| `.claude/skills/ads` | 네이버 쇼핑검색광고 운영 규칙 (설정·ROAS·입찰·소재 반복) | — |

> ✅ **재고앱 통합 완료 (2026-08-05)**: 원래 두 개였던 재고앱(`index.html`/`stock-cost/`)을
> `index.html` 하나로 합쳤다. `stock-cost/`가 갖고 있던 원재료 재고, 생산배치 원가·수율 저장,
> 원재료 매입/조정, 완제품 재고 조정은 모두 이식 완료(§3~§4). 실사용 데이터가 없던
> `stock-cost/`는 이식 후 삭제했다. **이식하지 않은 기능**(판매 채널수수료 자동계산, 월별 요약,
> 증빙 종류 기록, B2C/B2B 가격결정 가이드)이 필요해지면 git 이력의 `stock-cost/GUIDELINES.md`
> (삭제 전 커밋)를 참고해 `index.html`에 새로 구현한다.

**이 저장소 전체를 관통하는 사업 컨텍스트**
- 일반과세자(부가세 10%), 사업자번호 709-39-01334, 2025-07 개업.
  업태: 반려동물용 사료 제조업 + 통신판매업(전자상거래).
- 주력: 프리미엄 한우 우피껌(수제 소량 생산 → 배치별 원가·수율 관리가 핵심).
- 기준 단가(원): 우피 15,000/kg · 스틱 150/개 · 포장 450/세트 ·
  시급 10,000 · 건조 전기 500/h — 도구 설정에서 변경 가능하므로 코드 기본값과 문서를 함께 갱신.
  (식초는 원재료 목록에서 제외됨 — 2026-08-05)
- 관점: 숫자·증빙·비용효율 우선. 관리회계(앱)와 세무 증빙(홈택스 원본)을 혼동하지 않게 문구 작성.

**아래 §1부터는 `index.html`(현장재고) 전용 규칙**이다.
`detail/`을 고칠 때는 그 폴더의 `GUIDELINES.md`를 따른다.

---

## 1. 프로젝트 개요 (현장재고 · index.html)

- **무엇**: 펫 간식(우피 츄잉 등) 자체 생산·판매 + 맘엔펫 제품 납품을 하는 2인(형/동생)이
  현장에서 스마트폰으로 쓰는 **재고 관리 웹앱**. UI 언어는 한국어.
- **화면**: 하단 탭 4개 — 재고 / 입출고 / 이력 / 원가.
- **사용 환경**: 모바일 우선. 레이아웃 최대 폭 520px(`.wrap`), viewport 확대 금지 설정.

## 2. 스택과 아키텍처

- **현장재고 도구는 `index.html` 파일 하나** (HTML + CSS + JS 전부 인라인).
  빌드 단계·패키지 매니저·프레임워크·번들러 없음. (다른 도구 `detail/`도 HTML 1개다.)
- **호스팅**: GitHub Pages (정적 서빙). `main` 브랜치에 push하면 그대로 배포된다.
  별도 배포 스크립트나 CI 없음.
- **백엔드**: Firebase **Firestore** (JS SDK **v10.12.0**, gstatic CDN에서 dynamic `import()`).
  익명 인증(`signInAnonymously`)을 시도하며, 실패해도 계속 진행한다
  (보안 규칙이 `request.auth != null`을 요구한다는 가정의 주석이 코드에 있음).
- **오프라인/미설정 폴백**: `FIREBASE_CONFIG`가 비어 있거나 초기화 실패 시
  `localStorage`(키 `pf_state`) 단일 기기 모드로 동작하고 경고 배너(`#offBanner`)를 띄운다.
- **외부 리소스는 두 곳뿐**: Pretendard 폰트(jsdelivr CDN), Firebase SDK(gstatic CDN).

### [HARD RULE] 아키텍처 불변 조건
1. **단일 파일 유지** — 현장재고 도구는 `index.html` 하나로 유지한다. 이 도구를 위해
   js/css 분리·설정 파일·프레임워크·npm 등을 만들지 않는다. (다른 도구를 이 저장소에
   폴더로 추가하는 것과는 별개다.) 문서(md) 추가도 사용자가 요청했을 때만.
2. **이중 스토리지 유지** — 데이터 읽기/쓰기 기능을 추가·수정할 때는 반드시
   Firestore 분기(`FS` truthy)와 localStorage 분기(`FS` null) **둘 다** 구현한다.
   한쪽만 고치고 끝내지 않는다.
3. **`FIREBASE_CONFIG` 블록(`▼▼▼` ~ `▲▲▲` 주석 사이)은 사용자 관리 영역** —
   값을 바꾸거나 지우지 않는다. 이 `apiKey`는 Firebase 웹앱의 공개 클라이언트 식별자로,
   비밀키가 아니다. "키가 노출됐다"며 삭제·마스킹·회전을 제안하거나 실행하지 말 것.
   실제 접근 제어는 Firestore 보안 규칙 + 익명 인증이 담당한다.
4. **재고 수량 변경은 원자적으로** — Firestore 모드에서 재고 증감은 반드시
   `writeBatch` + `FS.increment(delta)`를 쓴다 (`commitMove`, `removeLog` 참고).
   stock을 읽어서 계산 후 덮어쓰는(read-modify-write) 코드를 작성하지 않는다.
   두 사용자가 동시에 입력하는 상황이 이 앱의 핵심 시나리오다.
5. **Firebase SDK 버전(10.12.0)을 임의로 올리지 않는다.** 올려야 한다면
   app/firestore/auth 세 모듈을 같은 버전으로 함께 올리고 사용자에게 먼저 알린다.

## 3. 데이터 모델 (Firestore)

| 컬렉션 | 문서 ID | 필드 |
|---|---|---|
| `products` | 시드 배열 인덱스 문자열 `"0"`~`"8"` | `name`(문자열), `stock`(숫자), `order`(숫자, 정렬용), `min`(숫자, 선택 — 적정재고. 없으면 기본값 `LOW=20`) |
| `materials` | 고정 3종 `"woopi"`\|`"stick"`\|`"pack"` | `stock`(숫자)만 저장. 이름·단위·기준치(`min`)는 DB에 없고 코드 상수 `MAT_META`/`MAT_ORDER`로 관리(SEED/LOW와 같은 패턴). 최초 접속 시 비어 있으면 3종 모두 `stock:0`으로 1회 시드. |
| `logs` | 자동 생성 ID | 제품 이동: `pid`, `pname`, `type`(`'in'`\|`'out'`\|`'adj'`), `qty`, `chan`, `user`, `memo`, `ts`, `date`. `'in'`/`'out'`은 `qty`가 항상 양의 크기(부호는 `type`이 결정)이고, `'adj'`(완제품 재고 조정)는 `qty`에 부호가 이미 포함된다(감소는 음수). 원재료 매입/조정은 `kind:'material'`, `matId`, `matName`, `mkind`(`'buy'`\|`'adj'`), `qty`(부호 있는 증감), `user`, `memo`, `ts`, `date`. 생산배치로 생긴 `type:'in'` 항목은 추가로 `matUsed`(투입 원재료 스냅샷 `{woopi,stick,pack}`), `unitCost`, `yieldPct`(선택)를 가진다. |

- `products`는 최초 접속 시 컬렉션이 비어 있으면 `SEED` 배열로 1회 시드된다.
- `logs` 실시간 구독은 `ts` 내림차순, `limit(300)`.
- localStorage 모드의 `pf_state`는 `{products, logs, materials}` JSON이며 필드 구조는 위와 동일
  (로그 ID만 `'l'+Date.now()` 형식).
- 필드 추가·이름 변경은 **양쪽 모드와 기존 저장 데이터 호환**을 함께 고려해야 한다.
  기존 문서에 없는 필드는 `p.min!=null ? p.min : LOW`처럼 기본값 폴백으로 읽는 것이 이 코드의 관례.
- `logs`의 `kind`/`matUsed` 필드는 옵션 필드다. 기존 데이터(둘 다 없음)는 자동으로 "제품 이동" 항목으로 렌더링되므로 하위 호환이 깨지지 않는다.

## 4. 도메인 규칙 (코드에 박힌 비즈니스 로직)

- **분류**: 제품 이름이 `'맘엔펫'`으로 시작하면 `buy`(맘엔펫 납품/매입), 아니면 `made`(자체생산/주력).
  분류는 **이름 접두사로 판정**한다(`categoryOf`). 제품명을 바꿀 때 이 규칙이 깨지지 않는지 확인할 것.
- **단위**(`unitInfo`): 맘엔펫 제품은 `box`, 그 외는 `EA`.
  맘엔펫 중 이름에 `알래스카연어`가 들어가면 box↔ea 환산 없음(`per:0`), 나머지 맘엔펫은 `1box = 12ea`.
- **재고 상태**: `stock <= 0` → 품절(out), `0 < stock <= min` → 부족(low). 기본 적정재고 `LOW = 20`.
  제품별 `min`을 0으로 두면 알림 대상에서 제외된다.
- **판매처 채널**(출고 시): 네이버스토어 / 쿠팡 / 펫프렌즈 / 기타. 입고는 채널 대신 `'생산입고'` 고정,
  재고 조정은 `'재고조정'` 고정.
- **담당자**: `형` / `동생` 두 명뿐. 기본값 `형`.
- **완제품 재고 조정**: 재고 탭 상단 "🔧 재고 조정" 버튼. **입력값은 증감분이 아니라
  실사로 확인한 실제(절대) 수량**이다 — 제품 선택 시 현재 재고로 자동 채워지므로,
  현장에서는 눈에 보이는 숫자를 그대로 고쳐 입력하면 된다(`onAdjProductChange`).
  저장 시(`saveAdjModal`) `newVal - stock`으로 delta를 계산해 기존과 동일하게
  `logs`에 `type:'adj'`, 부호 있는 `qty`로 남긴다 — 이력·CSV·`removeLog` 복원 로직은
  delta 기반이라 그대로 재사용된다. 실사 차이·파손·폐기 등에 쓰며 **사유가 없으면
  저장되지 않는다**. `commitMove`를 그대로 재사용해 원자적으로 처리한다(§2 HARD RULE 4).
  이력 탭에서 삭제하면 `removeLog`가 재고를 원래대로 되돌린다.
- **원가 탭**: 배치 단위 제조원가 계산기(재료비+인건비+전기, 실시간 미리보기). 마진은 부가세 제외 공급가 기준.
  **단가표(기준 단가) 입력값은 저장되지 않고** 입력할 때마다 즉석 계산된다(의도된 동작) — 이 부분은 예전 그대로다.
  반면 **"생산 배치 저장" 버튼을 누르면 실제로 저장**된다: 원재료 재고 차감 + 선택한 자체생산 제품 재고 증가 +
  이력(`logs`)에 원가·수율과 함께 기록, 모두 `commitBatch`로 원자적 처리(§2-4 HARD RULE 4).
  → 재고연동 없이 숫자만 가늠해보고 싶다면 저장 버튼을 누르지 않고 미리보기만 보면 된다.
- **원재료(우피/스틱/포장) 매입·조정**: 원가 탭 상단 "＋ 매입 입력"/"🔧 조정" 버튼.
  매입은 항상 양수로만 더해지고, 조정은 폐기·시식 증정·실사 차이 등에 쓰며 **사유가 없으면 저장되지 않는다**
  (`saveMatModal`). 두 경우 모두 `logs`에 `kind:'material'` 항목으로 남고, 이력 탭에서 삭제하면
  `removeLog`가 원재료 재고를 원래대로 되돌린다(생산배치 삭제 시 `matUsed`로 원재료도 함께 복원되는 것과 동일한 원리).
- **CSV 내보내기**: BOM(`﻿`) 포함 — 한글 엑셀 호환용. 제거하지 말 것. `kind==='material'` 항목은
  기존 CSV 컬럼(제품/판매처 등)에 맞지 않으므로 내보내기에서 제외한다.

## 5. 코드 관습

- **일반 `<script>`(모듈 아님) + 전역 함수 + 인라인 `onclick`** 스타일.
  새 핸들러도 이 방식을 따른다. `<script type="module">`로 바꾸면 인라인 핸들러가 전부 깨진다.
- DOM 접근은 `document.getElementById`, 렌더링은 `render()` 한 함수가 전체 화면을 다시 그린다.
  상태 변경 후에는 (Firestore 모드에선 `onSnapshot`이, 로컬 모드에선 직접 호출로) `render()`가 돌게 한다.
- **[HARD RULE] 사용자 입력 문자열을 `innerHTML`에 넣을 때는 반드시 `esc()`를 거친다**
  (제품명, 메모, 채널, 담당 등). 기존 코드가 이미 지키고 있는 XSS 방어선이다.
- CSS는 `:root` 커스텀 프로퍼티(`--brand`, `--out`, `--warn` 등) 기반의 브라운/크림 팔레트.
  새 색상은 하드코딩하지 말고 기존 변수를 재사용하거나 `:root`에 추가한다.
- 숫자 표시는 `tabular-nums` + `toLocaleString()`.
- 사용자에게 보이는 모든 문구·에러 메시지는 **한국어**.
- 비동기 저장 버튼 패턴: 누르면 `disabled` + "저장 중…" → `try/catch`로 실패 시
  한국어 alert/인라인 에러 → `finally`에서 복구. 새 저장 동작도 이 패턴을 따른다.

## 6. 완료의 정의 (Definition of Done)

배포 전 점검의 상세 체크리스트(브라우저 확인·콘솔 에러·탭 4개·입출고 흐름·로컬 모드·
모바일 뷰포트 등)는 **`.claude/skills/deploy-check`가 원본**이다 — 여기 다시 옮겨 적지 않는다.
그 체크리스트를 하나라도 통과 못 했으면 완료라고 보고하지 말고 "무엇을 확인 못 했는지"를 명시한다.

deploy-check 체크리스트와 별개로, 이 문서의 HARD RULE 위반 여부는 항상 재확인한다:
이중 스토리지(§2-2), `FIREBASE_CONFIG` 불변(§2-3), batch+increment(§2-4), `esc()`(§5).

### 커밋·배포
- 관측된 커밋 관습: 제목 한 줄 + 필요 시 한국어 본문 요약 (예: "재고부족 알림·동시수정·보안 개선").
  제목만 "Update index.html"로 두더라도 **본문에 무엇을 왜 바꿨는지 한국어로 한 줄** 남긴다.
- `main`에 push = 즉시 배포임을 항상 인지할 것. 확신 없는 변경은 브랜치에서 작업하고
  사용자에게 확인받는다.

## 7. 하지 말 것 (요약)

- 파일 분리, 프레임워크/빌드도구 도입, `type="module"` 전환 — [HARD RULE 1, §5]
- `FIREBASE_CONFIG` 수정·삭제, apiKey를 비밀로 취급해 지우기 — [HARD RULE 3]
- 재고 read-modify-write — [HARD RULE 4]
- Firestore 분기만/로컬 분기만 고치기 — [HARD RULE 2]
- `esc()` 없는 innerHTML 삽입 — [HARD RULE, §5]
- CSV의 BOM 제거, UI 영어화, 근거 없는 SDK 버전 업
- 이 문서에 없는 "선호"를 추측해서 적용하기 — 애매하면 작업을 멈추고 사용자에게 질문한다.

## 8. 알려진 한계 / 미확정 사항 (작업 시 참고)

- GitHub Pages 활성 여부·커스텀 도메인은 저장소 안에서 확인 불가(설정 화면 정보). 배포 관련
  작업 전에 사용자에게 확인.
- Firestore 보안 규칙의 실제 내용은 이 저장소에 없다(Firebase 콘솔 관리).
  규칙에 의존하는 변경(새 컬렉션 등)은 사용자에게 규칙 수정 필요 여부를 알릴 것.
- 자동화된 테스트 없음 — `.claude/skills/deploy-check`의 수동 체크리스트가 유일한 검증 수단.
- `removeLog`는 로그 전체를 `JSON.stringify`해 onclick에 인라인으로 넘긴다. 로그 필드를
  추가하면 이 경로(따옴표 이스케이프 포함)도 함께 검증할 것.
